# STATUS — HourStep Desktop 진행 상황

> 이 파일이 **현재 상태의 단일 출처(single source of truth)** 다.
> 작업을 이어갈 때 가장 먼저 읽고, 작업이 끝나면 여기부터 갱신한다.
> 배경·규칙은 `CLAUDE.md`, 상세 기록은 `docs/daily/`·`docs/decisions/`.

**마지막 갱신: 2026-08-07** · **현재 Phase: D1 완료 → 다음 D2**

---

## 완료

- **D0 구현 완료 (2026-08-05)** — 스캐폴딩 / 트레이 상주 / 부팅 자동실행 / 오버레이 스파이크 / NSIS·MSI 빌드 확인.
  일지 `docs/daily/2026-08-05-phase-d0.md`
- **D0 검증 자동화 완료 (2026-08-05)** — 검증 정책 수립 + `--debug-cmd` 훅 구축.
  일지 `docs/daily/2026-08-05-d0-verification.md` / 결정 `docs/decisions/0002-debug-cmd-hook.md`
- **D0 수동 검증 완료 (2026-08-07)** — 남은 4건 전부 통과, 실패 항목 없음.
  B-2'/B-4b(실클릭 전달·포커스 유지), C-1/C-3~C-5(전체화면 — **배타적 전체화면 게임에도 표시됨**),
  E-4'(트레이 아이콘 즉시 소멸), A(재부팅 자동 상주).
  일지 `docs/daily/2026-08-07-d0-verification-manual.md`
- **D1 완료 (2026-08-07)** — 세션 + 스케줄러 코어 루프. 하루 풀 세션 도그푸딩 가능.
  일지 `docs/daily/2026-08-07-phase-d1.md`
  - 도메인 타입 / `computeNextOccurrences` 순수 함수 + vitest 21개 / 프리셋 루틴 3종
  - 오버레이 실배선 (Behavior 주입, 3액션 → CompletionLog, 완료 후 선택형 1분 카운트다운)
  - **「알려진 한계 1」 해소** — 창을 카드 실크기로 축소. 실측 `(690,0) 540x139`,
    카드 밖 좌표 히트테스트가 전부 하위 창 반환 (D0: `(640,0) 640x240`)
  - single-instance 적용 (**「알려진 한계 3」 해소**)

## 진행 중

없음. **다음 작업은 D2.**

### 남은 자투리 (D2를 막지 않음)

- **C-4/C-5 게임명 미기록 + C-5 가 실제 배타적 전체화면이었는지 미확증.**
  옵션에 `전체화면` 이라 써 있어도 DXGI flip model 이면 실제로는 borderless.
  D3 「전체화면 감지」 착수 전에 게임 2~3종으로 보강할 것
- **카드 문구에 연구 인용이 없다.** 규칙 6이 "연구 인용형만" 이라 실제 출처 없이 지어내지 않았다.
  인용할 연구가 정해지면 `src/constants/strings.ts` 의 `BEHAVIOR_MESSAGE` 에 한 문장씩 추가
- **카운트다운 [좋아요] 이후 경로는 눈으로 확인 필요.** `done` 직후 제안 단계 유지까지만 자동 검증됨
- **릴리스 번들 재빌드 안 함.** D1 변경 후 `pnpm tauri build` 를 한 번도 안 돌렸다

## 미결 결정

없음.

## 다음 (D2)

- SQLite(`tauri-plugin-sql`) 로 CompletionLog·WorkSession 영속화 — 지금은 TS 인메모리
- 통계: 오늘/주간 작업시간 + 행동별 실천율. 세션 종료 시 요약 표시
- 설정 화면 확장: 행동별 on/off, 간격 조정
- 세션 미시작 리마인더

## 알려진 한계 (현재 남아 있는 것)

1. ~~오버레이 투명 영역이 클릭을 가로챈다~~ → **D1에서 해소**
2. 배타적 전체화면 게임 위 표시 — 2026-08-07 실측에서는 떴다. 게임명·모드 확증은 D3 재확인 대상
3. ~~중복 실행 방지 없음~~ → **D1에서 해소** (`tauri-plugin-single-instance`)
4. **오버레이는 항상 주 모니터.** 커서/활성 창 기준 배치는 D3에서 결정
5. **키보드 조작 불가.** `WS_EX_NOACTIVATE` 때문에 카드가 키보드 포커스를 받지 않는다.
   포커스 유지와 맞바꾼 것 — `src-tauri/src/overlay.rs` 의 `KEEP_FOCUS_ON_CLICK` 로 뒤집을 수 있다
6. **동시 due 는 큐로 밀린다.** 60분에 물마시기·눈휴식이 겹치면 하나씩 순서대로.
   2분 이상 밀린 것은 소진 처리(`STALE_MS`) — 절전 복귀 시 카드가 몰려 뜨는 것 방지
