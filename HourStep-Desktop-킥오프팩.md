# HourStep Desktop — 킥오프 팩 (Windows 우선)

> 컴퓨터를 켜면 조용히 상주하다가, "작업 시작"을 누르면 루틴대로 화면에 알림을 띄워
> 스트레칭·물·눈휴식을 실천시키는 트레이 앱. 본인 도그푸딩이 1차 목표.

---

## 0. 핵심 결정

| 결정 | 내용 | 근거 |
|---|---|---|
| DD1. 프레임워크 | **Tauri v2** (Rust 셸 + 웹 프론트) | 상시 상주 앱이라 메모리가 생명 — Electron ~150MB vs Tauri ~20MB. 트레이/자동실행/always-on-top 전부 공식 플러그인으로 해결(Rust 직접 작성 최소). 스케줄러 TS 로직을 모바일과 공유 가능 |
| DD2. 플랫폼 | **Windows 먼저**, macOS는 이후 | 본인 개발 PC 기준. Tauri라 mac 포팅 비용 낮음 |
| DD3. 세션 모델 | 열품타식: **작업 시작/종료 버튼** 기반 | 통계 자동 확보, 회의 문제 해소, 프리랜서 커버 |
| DD4. 알림 강도 | 3단계: 토스트(구석) < **카드(중앙 상단, 기본)** < 풀스크린 딤 | 행동별로 강도 선택 가능. 무시 못 하되 작업 파괴는 안 함 |
| DD5. MVP 범위 | **AI 없음, 계정 없음, 로컬 전용** | 프리셋 루틴 + 수동 설정. Supabase/AI/통합통계는 도그푸딩 검증 후 |
| DD6. 배포 | NSIS 인스톨러 (.exe) 직배포 | 스토어 불필요. 자동 업데이트는 P2 |
| DD7. UX 벤치마크 | Stretchly (오픈소스, Electron) | 강제 휴식 UX 레퍼런스. 단 우리는 세션 모델 + 한국어 + 다행동 루틴으로 차별화 |

## 1. MVP 사용자 플로우

```
부팅 → 트레이에 조용히 상주 (자동 시작)
 └ 트레이 클릭 → 미니 패널: [▶ 작업 시작] / 오늘 요약 / 설정
작업 시작 → 세션 타이머 가동 → 루틴 엔진 시작
 └ 50분 경과 → 중앙 상단 카드 슬라이드 인:
    "🧘 일어나서 1분 스트레칭할 시간이에요"  [✅ 완료] [⏰ 3분 뒤] [건너뛰기]
    └ 완료 탭 → 1분 카운트다운(선택) → 기록 → 다음 알림 예약
작업 종료 → 오늘 요약 토스트: "4시간 20분 작업, 휴식 6회 실천 (85%)"
```

## 2. Phase 플랜

**Phase D0 — 스캐폴딩 & 상주 스파이크**
- Tauri v2 + React + TypeScript + Vite 셋업
- 플러그인: tauri-plugin-autostart(부팅 자동실행), 트레이 아이콘 + 메뉴
- **오버레이 스파이크**: 프레임리스·always-on-top·작업표시줄 미표시·클릭 가능한 알림 창을 임의 시점에 띄웠다 닫기. 게임/영상 풀스크린 위에서도 뜨는지 확인
- ✅ 완료 조건: 재부팅 후 트레이 자동 상주 + 버튼으로 오버레이 카드 표시/액션 동작

**Phase D1 — 세션 + 루틴 엔진 (코어)**
- 도메인: WorkSession { startedAt, endedAt } / Behavior { name, icon, intervalMin | atElapsedMin[], intensity } / CompletionLog
- 스케줄러 순수 함수: computeNextOccurrences(session, behaviors, now) — **vitest 단위 테스트 필수** (모바일 공유 전제로 IO 제로)
- 오버레이 카드 3액션(완료/3분 뒤/건너뛰기) + 완료 시 선택형 1분 카운트다운
- 프리셋 루틴 1종 내장: 50분마다 스트레칭 / 30분마다 물 / 60분마다 눈휴식 1분
- ✅ 하루 풀 세션 도그푸딩 가능

**Phase D2 — 통계 + 설정**
- 오늘/주간 요약: 총 작업시간, 휴식 횟수·실천율, 행동별 완료율 (로컬 SQLite: tauri-plugin-sql)
- 설정: 행동 on/off·간격·강도(토스트/카드/풀스크린), 자동시작 토글, "평소 시작 시간에 세션 미시작 시 리마인더"
- 건강 효용 문구: 단정 금지, **연구 인용형**("~라는 연구가 있어요")으로만
- ✅ 통계 화면 보고 루틴을 조정하고 싶어지는가 (자가 평가)

**Phase D3 — 폴리싱 & 배포**
- 풀스크린 앱 감지 시 알림 지연 옵션(발표/게임 보호), 다중 모니터 위치 처리
- NSIS 인스톨러 빌드, 아이콘/브랜딩
- ✅ 깨끗한 PC에 설치 → 재부팅 → 세션 한 사이클 성공

## 3. CLAUDE.md (데스크톱 리포 루트용)

```markdown
# HourStep Desktop — 세션 기반 웰니스 브레이크 앱 (Windows)

## 제품 한 줄
트레이 상주 앱. "작업 시작"을 누르면 루틴(스트레칭/물/눈휴식)대로
화면 중앙 상단에 무시하기 어려운 오버레이 카드를 띄워 실천시킨다.

## 핵심 결정 (변경 시 사용자와 논의)
- Tauri v2 + React + TS + Vite. Rust 코드는 최소화, 가능하면 공식 플러그인 사용
- 세션 모델: 알림은 활성 WorkSession 중에만 발생. 고정 근무시간 개념 없음
- 오버레이 기본 강도는 "중앙 상단 카드". 풀스크린 딤은 행동별 opt-in
- 스케줄러는 IO 없는 순수 함수 + vitest. 모바일(Expo)과 로직 공유 전제 —
  Date 주입, 플랫폼 API 참조 금지
- MVP는 로컬 전용(SQLite). 계정/Supabase/AI 금지 (이후 Phase)
- 건강 효용 문구는 연구 인용형만, 치료·개선 단정 표현 금지
- git commit·릴리스 빌드는 사용자가 직접 수행
- UI 문구는 한국어, 상수 파일 분리. 사용자가 쓴 카피는 임의 수정 금지

## 검증 환경
Windows 11 데스크톱 (개발 PC = 도그푸딩 기기)
```

## 4. Phase D0 실행 프롬프트

```text
HourStep Desktop의 Phase D0를 시작한다. CLAUDE.md를 먼저 읽어라.
목표는 제품이 아니라 "상주 + 오버레이" 기술 검증이다.

T1. Tauri v2 + React + TS + Vite 스캐폴딩. pnpm 사용.
T2. 트레이: 아이콘 + 메뉴(작업 시작 placeholder / 설정 / 종료).
    메인 창은 닫아도 앱이 종료되지 않고 트레이로 숨을 것.
T3. tauri-plugin-autostart로 부팅 자동 실행 + 설정에서 토글.
T4. 오버레이 스파이크: 프레임리스 + always-on-top + skipTaskbar 창을
    화면 중앙 상단에 슬라이드 인으로 표시. 카드 내용: 행동 이름 +
    [완료][3분 뒤][건너뛰기] 버튼 → 클릭 로그 콘솔 출력 후 닫힘.
    트레이 메뉴에 "테스트 알림" 항목으로 수동 트리거.
T5. docs/phase-d0-verification.md: 재부팅 자동상주 / 오버레이가
    브라우저·전체화면 영상 위에 뜨는지 / 다중 모니터에서 주 모니터에
    뜨는지 수동 확인 절차 작성.

금지: 세션/스케줄러/통계/설정 UI 구현 금지 (D1~D2 범위).
완료 조건: pnpm tauri dev로 T2~T4 동작 + tauri build로 exe 생성 확인.
```

## 5. 모바일과의 관계 (전선 관리)

- 모바일 리포는 **동결하되 버리지 않음** — Phase 0(스캐폴딩+Supabase auth) 커밋까지 완료된 상태로 보존
- 데스크톱 D1의 스케줄러 순수 함수가 사실상 **모바일 Phase 1의 선행 구현**이 됨 → 재개 시 복사 이식
- 통합 통계(비전)는 데스크톱 검증 후 Supabase 동기화 Phase에서 착수
- 재개 우선순위 신호: 데스크톱 도그푸딩에서 "외출 중에도 이어지면 좋겠다"가 체감되는 순간

#기획 #hourstep
